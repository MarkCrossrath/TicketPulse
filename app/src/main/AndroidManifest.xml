<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ticketpulse">
    <!-- add internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- External storage permission -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Wallpaper permission -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminMenuActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".TicketSelectionActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AdminEventListActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".Ticket1Activity" />
        <activity android:name=".Ticket2Activity" />
        <activity android:name=".Ticket3Activity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".MyTicketActivity" />
        <activity android:name=".TicketsForSaleActivity" />
    </application>

</manifest>